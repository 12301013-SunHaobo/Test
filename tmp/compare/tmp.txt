    @Resource
    private SecureSessionRepository<SecureSession> _secureSessionRepository;
    
    // this is a reference to a Spring PROXY that proxies a per-request object
    @Resource
    private SecureSessionHolder _sessionHolder;
    
    @Resource
    private MASAuthenticator _masAuthenticator;
       
    @Resource(name="ssoService")
    private SSOService _ssoService;
    